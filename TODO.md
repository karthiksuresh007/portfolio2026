# Portfolio Build Notes

## Immediate content pass
- Replace `#github-profile` and `#linkedin-profile` in `src/content/site.ts` with verified public URLs.
- Swap the three flagship placeholder case studies with real projects from the resume and GitHub.
- Replace placeholder metrics with resume-verified numbers only.
- Add a verified email or contact workflow before publishing.

## Launch polish
- Set real production URL in `src/app/sitemap.ts` and `src/app/robots.ts`.
- Add analytics provider if you want `trackEvent` calls to report anywhere.
- Replace or expand the SVG art with real screenshots once project stories are finalized.
- Run a final copy review against the resume PDF before deployment.
